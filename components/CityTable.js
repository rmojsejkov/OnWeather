import React from 'react';
import {
    Text,
    View,
    StyleSheet,
    ScrollView
} from 'react-native';

const CityTable = props => {
    return(
        <ScrollView>

        </ScrollView>
    );
};

const styles = StyleSheet.create({
    screen: {

    }
});

export default CityTable;